# Copy to vars.local.nix in this directory (gitignored).
# Set NIXOS_CONFIG if your checkout is not at ~/nixos.
#
# Use this for machine-specific overrides only.

{
  # desktop = "hyprland";
  # noctalia = "/path/to/noctalia";
}
